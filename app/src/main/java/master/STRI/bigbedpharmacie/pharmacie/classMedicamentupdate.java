package master.STRI.bigbedpharmacie.pharmacie;

public class classMedicamentupdate {

    /*
itemView.setOnLongClickListener(new View.OnLongClickListener() {
                @Override
                public boolean onLongClick(View v) {
                    AlertDialog.Builder builder=new AlertDialog.Builder(context);
                    builder.setTitle(R.string.are_you_sure);
                    builder.setMessage(R.string.deleting_is);
                    builder.setPositiveButton(R.string.yes, new DialogInterface.OnClickListener() {
                        @Override
                        public void onClick(DialogInterface dialog, int which) {
                            Toast.makeText(context,R.string.deleting,Toast.LENGTH_LONG).show();

                            /*
                            Med_Info med=listmed.get(getPosition());
                            firestore.collection("Services").document(med.getId()).delete().addOnSuccessListener(
                                    new OnSuccessListener<Void>() {
                                        @Override
                                        public void onSuccess(Void aVoid) {
                                            Toast.makeText(context,R.string.deleting,Toast.LENGTH_LONG).show();
                                            context.startActivity(new Intent(context, updateService.class));
                                        }
                                    }
                            );


                        }
                                });
                                builder.setNegativeButton(R.string.no, new DialogInterface.OnClickListener() {
@Override
public void onClick(DialogInterface dialog, int which) {

        }
        });
        AlertDialog boum=builder.create();
        boum.show();
        return false;
        }
        });
 */


}
